Team Keyboard:: Jonathan Wu, Angela Zhang
SoftDev pd2
K32 -- more canvas based JS animation
2022-02-17t
time spent: 60 minutes

- You can create a function with the addEventListener in of itself to call more functions for more complicated conditionals and interactions in JS.
- ctx.drawImage has two functions, one with x,y variables and one with four variables. The one with two variables uses the default size of the image while the one with four variables considers the scaling of the image as well.
